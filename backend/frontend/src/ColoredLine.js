import React from 'react';

//horizontal line
const ColoredLine = ({ color }) => (
    <hr
        style={{
            height: 0.5,
            opacity: 0.5
        }}
    />
);


export default ColoredLine;